//here I'll be actually adding the code to fetch the comments of the user 
//the tech that I am gonna use here is called socket.io
$(()=>{
    function(artistic){
        console.log("server started at http://localhost:4343");
        $.get('local',()=>{
            console.log(console output in the demand);
            var x=arr.get(x);
            
        })
    }
})